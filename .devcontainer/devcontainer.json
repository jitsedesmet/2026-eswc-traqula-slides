// See https://containers.dev/implementors/json_reference/ for configuration reference
{
  "name": "Untitled Node.js project",
  "build": {
      "dockerfile": "Dockerfile"
  },
  "mounts": [
    "source=${localWorkspaceFolder}/.devcontainer/config.json,target=/home/node/.copilot/config.json,type=bind",
  ],
  forwardPorts: [8080],
  "remoteUser": "node",
  "customizations" : {
    "jetbrains" : {
      "backend" : "WebStorm"
    }
  }
}
